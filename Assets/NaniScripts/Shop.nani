@skipInput
@back Shop

@showUI DrinkUI
@set questText="Find a drink"
@goto .DrinkLoop

# DrinkLoop
@wait 0.5 
@if drinkTaken
    @goto .Continue
@else
    @goto .DrinkLoop
@endIf

#Continue
@printer Dialogue
Go back to GYM using mapp[skipInput]
@stop